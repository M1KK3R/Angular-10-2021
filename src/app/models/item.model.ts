export class Item {
    constructor(
        public title: string, 
        public price: number, 
        public category: string, 
        public imgSrc: string, 
        public isActive: boolean,
    ) {
    }
}

