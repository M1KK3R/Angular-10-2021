import { DynamicHeightDirectiveDirective } from './dynamic-height-directive.directive';

describe('DynamicHeightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHeightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
